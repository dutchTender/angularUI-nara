'use strict';

describe('myApp.bu module', function() {

  beforeEach(module('myApp.bu'));

  describe('bu controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var buCtrl = $controller('buCtrl');
      expect(buCtrl).toBeDefined();
    }));

  });
});