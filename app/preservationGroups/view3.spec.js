'use strict';

describe('myApp.pg module', function() {

  beforeEach(module('myApp.pg'));

  describe('pg controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pgCtrl = $controller('pgCtrl');
      expect(pgCtrl).toBeDefined();
    }));

  });
});